options:
  organization: sales-demo
  repositoryName: FairwindsOps/insights-cicd-test
  setExitCode: false

images:
  docker:
    - demo-image

manifests:
  yaml:
    - ./passing.yaml
    - ./failing.yaml
  helm:
    - name: staging
      path: ./chart/
      variableFile: ./chart/values.yaml

